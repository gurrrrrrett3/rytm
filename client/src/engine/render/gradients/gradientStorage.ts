import Gradient from "../../util/colors/gradient";

export default class GradientStorage {
    public static gradients = {
        bgBeat: new Gradient([
            "#000000",
            "#222222",
        ])
    };
}